package org.example;

public final class ArgumentUtil {
    private ArgumentUtil() {}

    public static ... parseArguments(String[] args) {
        for (int i = 0; i < args.length; i++) {
            if (args[i].startsWith("-") && !args[i + 1].startsWith("-")) {

            }
        }
    }
}
